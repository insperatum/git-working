#!/bin/bash

git checkout master
git push origin master
git checkout working
git reset 'working@{1}' # This should be top of the working branch
git merge -X theirs --no-edit master
git push origin working
